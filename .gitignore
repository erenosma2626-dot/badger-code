# Credentials — never commit
.env
.env.*
!.env.example

# Reference-only clone (has its own git history, not part of this repo)
reference-starter-repo/

# Internal orchestration tooling — not part of the submitted coding agent
CLAUDE.md
.agent-system/
docs/plan.md

# Python
.venv/
__pycache__/
*.pyc

# Node (if LangChain tooling needs it)
node_modules/

# Harbor run artifacts
jobs/

# OS junk
.DS_Store
